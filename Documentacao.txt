=========================================================
Documentação: Simulador do Sistema Alerta Cidadão Conectado
Arquivo: alerta_cidadao_simulador.py
=========================================================

Índice:
1. Propósito do Script
2. Como Executar o Script
3. Parâmetros Globais Configuráveis
4. Visão Geral das Funções
   4.1. Funções de Validação de Entrada
   4.2. Funções do Sistema de Alerta
   4.3. Função Principal
5. Interação com o Usuário
6. Saída Esperada no Console

--------------------------------------
1. Propósito do Script
--------------------------------------
O script `alerta_cidadao_simulador.py` simula o backend (a lógica de processamento)
do projeto "Alerta Cidadão Conectado". Ele foi desenvolvido para a disciplina de
Computational Thinking Using Python.

Principais funcionalidades:
- Simula a leitura de dados de um sensor de nível de água.
- Permite que o usuário insira relatos de ocorrências relacionadas a enchentes.
- Valida as entradas de dados fornecidas pelo usuário.
- Calcula e exibe o status de alerta (Seguro, Atenção, Crítico, Falha Sensor)
  com base nos dados do sensor.
- Processa de forma simplificada os relatos da comunidade para gerar alertas adicionais.
- Apresenta todas as informações e alertas de forma textual no console.

--------------------------------------
2. Como Executar o Script
--------------------------------------
Para executar este script, você precisa ter o Python instalado em seu sistema.

1. Salve o código Python em um arquivo chamado `alerta_cidadao_simulador.py`.
2. Abra um terminal ou prompt de comando.
3. Navegue até o diretório onde você salvou o arquivo.
4. Execute o seguinte comando:
   python alerta_cidadao_simulador.py

O script iniciará e começará a interagir com você através do console.

--------------------------------------
3. Parâmetros Globais Configuráveis
--------------------------------------
No início do script, alguns parâmetros globais podem ser ajustados para
modificar o comportamento da simulação:

- `ALTURA_TOTAL_RESERVATORIO_CM` (float):
  Define a altura total (profundidade) em centímetros do local que está sendo
  monitorado (ex: bueiro, pequeno córrego). O valor padrão é 100.0 cm.
  Este valor é fundamental para calcular o percentual do nível da água.

- `LIMIAR_ATENCAO_PERCENT` (float):
  Define o percentual do `ALTURA_TOTAL_RESERVATORIO_CM` a partir do qual o sistema
  entra em estado de "Atenção". O valor padrão é 0.30 (30%).

- `LIMIAR_CRITICO_PERCENT` (float):
  Define o percentual do `ALTURA_TOTAL_RESERVATORIO_CM` a partir do qual o sistema
  entra em estado "Crítico". O valor padrão é 0.70 (70%).

- `DADOS_SENSOR_SIMULADOS` (list de floats):
  Uma lista de valores que simulam as leituras do sensor de nível de água em cm.
  O script ciclará por esta lista para simular a variação do nível da água ao
  longo do tempo e testar diferentes cenários de alerta (Seguro, Atenção,
  Crítico, Falha Sensor). Você pode modificar esta lista para criar suas próprias
  sequências de simulação.

--------------------------------------
4. Visão Geral das Funções
--------------------------------------

   4.1. Funções de Validação de Entrada
   ------------------------------------
   - `validar_entrada_texto_obrigatorio(prompt_usuario, tamanho_max=150)`:
     Solicita uma entrada de texto ao usuário, garantindo que não seja vazia
     e que não exceda um tamanho máximo. Continua solicitando até que uma
     entrada válida seja fornecida.

   - `validar_opcao_lista(prompt_usuario, opcoes_validas)`:
     Apresenta ao usuário uma lista de opções válidas (passadas como um
     dicionário) e solicita que ele escolha uma delas. Repete até que uma
     opção válida seja digitada.

   4.2. Funções do Sistema de Alerta
   ---------------------------------
   - `ler_dados_sensor_simulado(sensor_id="principal")`:
     Simula a leitura de um sensor de nível de água. Retorna o próximo valor da
     lista `DADOS_SENSOR_SIMULADOS` ou um valor aleatório se a lista estiver vazia.
     Inclui um pequeno atraso para simular o tempo de leitura.

   - `calcular_status_alerta_sensor(nivel_agua_cm, altura_total_cm, limiar_atencao_p, limiar_critico_p)`:
     Recebe o nível atual da água e os parâmetros do sistema, e retorna uma
     string indicando o status do alerta ("Seguro", "Atenção", "Crítico" ou
     "Falha Sensor"). A lógica é baseada nos percentuais de limiar definidos.

   - `coletar_relato_cidadao()`:
     Interage com o usuário para coletar detalhes de uma ocorrência comunitária
     (tipo, local, descrição). Utiliza as funções de validação para garantir
     a qualidade dos dados. Retorna um dicionário com os dados do relato ou
     `None` se o usuário cancelar.

   - `processar_relatos_para_alerta(lista_relatos)`:
     Analisa a lista de relatos comunitários. Nesta versão simplificada, gera uma
     mensagem de alerta se o relato mais recente for de um tipo considerado
     crítico (ex: "alagamento de via", "nível subindo rapidamente").

   - `exibir_mensagem_console(mensagem, tipo="INFO")`:
     Formata e exibe mensagens no console, prefixando-as com um indicador de
     tipo (INFO, ALERTA, ERRO, DESTAQUE) para melhor legibilidade.

   4.3. Função Principal
   ---------------------
   - `main_loop_simulador()`:
     Orquestra a execução do simulador. Contém o loop principal que executa
     os ciclos de monitoramento. Em cada ciclo:
       1. Lê os dados simulados do sensor.
       2. Calcula e exibe o status de alerta do sensor.
       3. Pergunta ao usuário se deseja registrar uma ocorrência comunitária.
       4. Se sim, coleta e processa o relato.
       5. Pergunta ao usuário se deseja continuar para o próximo ciclo.
     Encerra quando o usuário decide não continuar.

--------------------------------------
5. Interação com o Usuário
--------------------------------------
Durante a execução do script, o usuário interage com o sistema através do console:

- **No início de cada ciclo:** O script exibe o status atual do sensor.
- **Registro de Ocorrências:** O script perguntará "Deseja registrar uma ocorrência comunitária neste ciclo? (s/n):".
  - Se o usuário digitar 's' (sim), ele será guiado para fornecer:
    - O tipo da ocorrência (escolhendo de uma lista numerada).
    - A localização da ocorrência (texto livre).
    - Uma breve descrição da situação (texto livre).
    - Uma confirmação final para enviar o relato.
- **Continuação dos Ciclos:** Ao final de cada ciclo de monitoramento, o script perguntará "Continuar para o próximo ciclo de monitoramento? (s/n):".
  - Se o usuário digitar 's' (sim), um novo ciclo se inicia.
  - Se digitar 'n' (não) ou qualquer outra coisa, o simulador é encerrado.

As funções de validação garantem que o usuário forneça dados nos formatos esperados, solicitando novas tentativas em caso de erro.

--------------------------------------
6. Saída Esperada no Console
--------------------------------------
O script produzirá saídas de texto no console, incluindo:

- Mensagens de boas-vindas e encerramento.
- Indicador do ciclo de monitoramento atual.
- Leituras simuladas do sensor de nível de água (em cm).
- Status do sistema calculado com base na leitura do sensor (ex: "[INFO ℹ️] Status: Nivel SEGURO.", "[ALERTA 🔴] Status do Sistema (baseado no sensor): Crítico").
- Prompts para entrada de dados do usuário ao registrar ocorrências.
- Mensagens de confirmação ou cancelamento de registro de ocorrências.
- Alertas gerados a partir dos relatos comunitários (ex: "[ALERTA 🔴] Alerta Comunitário Urgente: ...").
- Mensagens de erro caso a entrada do usuário seja inválida.

As mensagens são prefixadas para indicar seu tipo (INFO, ALERTA, ERRO, DESTAQUE) para facilitar a leitura e compreensão do que está acontecendo na simulação.